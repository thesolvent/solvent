# --- Dev artifacts: local-only, never pushed to origin ---
docs/superpowers/
.claude/

# --- Node (1inch deps resolve via npm) ---
node_modules/

# --- Rust (backend, added later) ---
/backend/target
/target

# --- Foundry ---
/contracts/out
/contracts/cache
/contracts/broadcast

# --- env / secrets ---
.env
.env.*
!.env.example
*.log

# --- editor / OS ---
.DS_Store
.idea/
.vscode/
*.swp

# --- proptest failure-seed artifacts (regenerable, not tracked) ---
proptest-regressions/

.codegraph/

# --- local-only recapture scenario suite (Stager + Validator; kept out of git deliberately) ---
crates/adapters/tests/scenarios_recapture.rs
