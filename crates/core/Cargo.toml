[package]
name = "solvent-core"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
default = []
tracing = ["dep:tracing"]

[dependencies]
alloy-primitives = "1"
rust_decimal = "1"
thiserror = "2"
tracing = { version = "0.1", optional = true }
